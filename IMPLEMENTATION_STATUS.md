# 首版实现与验证记录

日期：2026-09-20。

## 已实现

- React、Vite、Tailwind、React Router 静态前端，无后端、账户或运行时数据请求。
- 中文首页、六回山川、六个章节、资料来源、关于项目；共十个主路由及中文错误页。
- 原创六回矢量意境、项目标志、印记图标；不使用受限游戏截图。
- 第一回经过独立成员交叉核对的路线示意图：12 个文化点、3 个连接点、15 条连线。隐藏传送、返回、领取关系与普通路径分开，地图常驻非地理比例说明。
- 第二至六回已补充高层路线示意图，覆盖各回主要区域、并行探索、条件传送、隐藏区域与奖励关系；归一化坐标只承担插画布局，不声称游戏地理比例。
- 章节共 47 张阅读卡，按游戏、原著、现实遗产分层；六回均可在地图中点选阅读，另保留没有伪造坐标的纯故事卡。
- 第三回亢金龙与玉皇庙的文化对照，图片作者、许可、原始链接、转换说明与证据范围齐全。
- 类型和知识层组合筛选、空状态、抓取拖拽、滚轮／双指缩放、复位与大图模式；章节栏可横滑，并提供前后回目箭头与方向过渡。
- 桌面抽屉、手机全屏阅读、键盘焦点恢复与减少动态效果支持。
- 所有中文阅读卡在构建时写入 HTML，原生折叠条目支持关闭脚本阅读；每条主路由有独立标题和可直达静态页面。
- 16 项数据门槛测试；严格 schema、源 ID、坐标、拓扑、权利和纪实配图验证已接入 prebuild 与 CI。

## 验证

- `npm run build`：数据校验、TypeScript、16 项测试、浏览器与服务端构建、10 路由预渲染通过。
- 浏览器逐一直接打开十个主路由：全部有中文静态正文，HTTP 200，无运行时错误。修正了 Vite 预览无尾斜线路径回退到首页造成的水合问题。
- 无 JavaScript 的手机上下文：第一回 13 张阅读卡在 DOM 中；观音禅院折叠卡可原生展开并读取原著内容。
- 组合筛选：器物剩 1 个图点；叠加现实遗产层显示中文空状态；恢复全部后重现 12 个图点。
- 抽屉：地图点选可打开；Esc 可关闭并恢复到原标记焦点；第一回卡片没有空的现实遗产区块。
- 手机 390×844：首页、地图页及遗产卡无页面横向溢出；阅读面板覆盖屏幕；纪实照片加载正常，署名与许可可见。
- 桌面 1440×1000：检查首页与地图视觉；初始地图比例按可用宽度和高度适配。
- 大图模式覆盖可用视口；滚轮由 99% 放大至 141%，拖拽改变地图位移，Esc 退出后页面滚动恢复。
- 章节侧箭头从第一回切至第二回，页面呈现向前切换过渡；手机章节栏保持原生横滑与吸附，滚动条隐藏。
- 深链接：第三回 `#kang-jin-loong` 自动展开对应正文。
- `git diff --check` 通过。浏览器截图和临时诊断产物在被忽略的 `output/playwright/`，不进入公开站点。

实机触屏双指手势尚未在真实手机硬件上验收；缩放库已启用其触摸交互。未声称做过完整人工无障碍审计或对所有历史资料进行新的学术核校。

## 仍待补充

详情见 `ASSET_GAPS.md`：第二至六回逐点实机地理复核、授权游戏画面、中文字体子集、更精细的原创山水插画、小西天可复用的内部悬塑照片。现有字体使用系统回退，不分发未经许可的字体文件。当前地图均为高层路线示意，不是官方地图或地理测绘。

本轮没有上线部署、提交或推送。`dist/` 是静态部署产物，`.ssr/` 仅用于构建，二者均被忽略。构建不会自动批量清理任何目录。

最终补充验证：手机键盘聚焦到原本位于视野外的黑熊精标记后，该点已自动进入地图可见区域；回车打开、Esc 关闭通过。拖拽平移的变换值发生预期改变；复位后可恢复初始视图。中文未知路径返回 HTTP 404。已将本机最终预览保持在 `http://127.0.0.1:4175/`。
